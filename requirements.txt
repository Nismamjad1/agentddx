# Core dependencies — everything needed to reproduce the paper.
# See requirements-demo.txt for the optional interactive demo.

# LLM backbone (OpenRouter is OpenAI-API compatible)
openai>=1.30.0
python-dotenv>=1.0.0

# Evidence retrieval
biopython>=1.83

# Benchmark loading and analysis
datasets>=2.16.0
numpy>=1.26.0
